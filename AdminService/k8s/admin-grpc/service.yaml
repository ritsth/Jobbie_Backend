apiVersion: v1
kind: Service
metadata:
  name: admin-grpc
  namespace: jobbie-microservices
spec:
  selector:
    app: admin-grpc
  ports:
    - port: 8080
      targetPort: 8080
      name: grpc
  type: ClusterIP
