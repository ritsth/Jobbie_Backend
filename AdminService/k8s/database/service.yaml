apiVersion: v1
kind: Service
metadata:
  name: mysql-adminservice-headless
  namespace: jobbie-backend
spec:
  clusterIP: None
  selector:
    app: mysql-adminservice
  ports:
    - port: 3306
      targetPort: 3306