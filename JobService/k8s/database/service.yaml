apiVersion: v1
kind: Service
metadata:
  name: mysql-jobservice-headless
  namespace: jobbie-backend
spec:
  clusterIP: None
  selector:
    app: mysql-jobservice
  ports:
    - port: 3306
      targetPort: 3306