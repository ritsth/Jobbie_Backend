apiVersion: v1
kind: PersistentVolume
metadata:
  name: job-mysql-pv-volume
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "/mnt/data/job-mysql"
  claimRef:
    name: job-mysql-pv-claim
    namespace: jobbie-backend
---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: job-mysql-pv-claim
  namespace: jobbie-backend
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi