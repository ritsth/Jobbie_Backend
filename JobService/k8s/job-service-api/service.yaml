apiVersion: v1
kind: Service
metadata:
  name: job-service-api
  namespace: jobbie-backend
spec:
  selector:
    app: job-service-api
  ports:
    - port: 80
      targetPort: 8080
      name: http
    - port: 443
      targetPort: 443
      name: https
  type: ClusterIP